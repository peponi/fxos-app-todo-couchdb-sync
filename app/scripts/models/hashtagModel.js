// 'use strict';

// import pm from './pouchModel';

// /* Fix Income Model */
// const HashtagModel = function() {
//     let type = 'hashtag',
//         self = this;

//     self.transaction = (formData) => {
//         pm.save(type, formData);
//     };

//     self.getAllHashtags = (model, functionName) => {
//         pm.getAll(type, (result) => {
//             model[functionName](result);
//         });
//     };
// }

// export default new HashtagModel();
